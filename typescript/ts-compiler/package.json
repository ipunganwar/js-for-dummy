{
  "name": "ts-compiler",
  "version": "1.0.0",
  "description": "## if only you want to run only specific directory/file without need quit and re-compiler, just using - tsc filename.ts --watch",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "lite-server"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "lite-server": "^2.6.1"
  }
}
